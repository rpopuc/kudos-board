components:
  schemas:
    PanelResponse:
      type: object
      required:
        - owner
        - title
        - slug
        - createdAt
      properties:
        slug:
          type: string
          format: uuid
          example: "Panel slug identifier"
        owner:
          type: string
          example: "Panel owner"
        title:
          type: string
          example: "Panel Title"
        description:
          type: string
          example: "Panel Description"
        createdAt:
          type: string
          format: date-time
          example: "Panel creation date"
